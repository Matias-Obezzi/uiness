---
"@uiness/island": minor
---

Initial release: `<Island />` renderer with spring morph, `island` store with `show`, `update`, `dismiss`, stacking, auto dismiss with hover pause, and built-in `confirm`, `alert` and `promise`.
