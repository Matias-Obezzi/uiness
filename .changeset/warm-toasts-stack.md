---
"@uiness/toast": minor
---

Initial release: `toast()` with success, error, info, warning, loading, promise and custom variants, and a `<Toaster />` with a stack that expands on hover, swipe to dismiss, paused timers, actions and six positions.
