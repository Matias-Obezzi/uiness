# Changesets

Run `pnpm changeset` after a change that should be released, pick the packages and the bump type,
and commit the generated file. `pnpm version` applies them, `pnpm release` builds and publishes.
