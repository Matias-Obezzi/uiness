import { TextGenerate } from '@/ui/text-generate'

export default function TextGenerateDemo() {
  return (
    <TextGenerate
      className="max-w-xl font-semibold text-2xl leading-snug"
      text="Words that arrive one after another, each one settling out of a blur, while the whole sentence stays plain text underneath."
    />
  )
}
