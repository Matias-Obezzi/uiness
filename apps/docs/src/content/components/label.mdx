<ComponentPreview name="label-demo" />

## Installation

<Install name="label" />

## Usage

```tsx
import { Label } from '@/components/ui/label'

<Label htmlFor="email">Your email</Label>
```

Labels fade when the control they point to is disabled, through the `peer` and `group` conventions.
